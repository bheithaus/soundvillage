<% if (SV.Store.currentUser.get("provider")) { %>
	<a class="btn btn-custom" id="fb-share">
		<img src="<%= SV.assets.imageUrl('facebook-icon.png') %>">
		<div class="text">
			Share on Facebook
		</div>
	</a>
<% } %>